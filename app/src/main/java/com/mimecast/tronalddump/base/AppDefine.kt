package com.mimecast.tronalddump.base

/**
 * Class to hold all the constants
 * We're using an object instead of companion to avoid
 * unnecessary creation of getter and setter methods
 */
object AppDefine {
    var BASE_URL = "https://api.tronalddump.io/"
    const val BUNDLE_KEY_TAG_STRING = "tag_key"
}